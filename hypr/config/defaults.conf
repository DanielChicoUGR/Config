# ┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
# ┃                     Defaults Configuration                  ┃
# ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛

$filemanager = thunar
# $applauncher = rofi -show combi -modi window,run,combi -combi-modi window,run
$applauncher = wofi -I --show drun,run 
$wofi-main-menu = ~/.local/bin/wofi-mainmenu
$wofi-wifi = ~/.local/bin/wofi-wifi-menu



# $applauncher = "$HOME/.config/wofi/wofi-mainmenu"
# $applauncher = "$HOME/.config/rofi/scripts/launcher_t7"
$terminal = kitty
# $idlehandler = swayidle -w timeout 300 'swaylock -f -c 000000' before-sleep 'swaylock -f -c 000000'
# $idlehandler = swayidle -w timeout 300 'swaylock' timeout 360 'hyprctl dispatch dpms off' resume 'hyprctl dispatch dpms on' before-sleep 'swaylock -f'

$idlehandler=swayidle -w  timeout 300 'swaylock -f -c 000000' timeout 600 'swaymsg "output * dpms off"' resume 'swaymsg "output * dpms on"'s before-sleep 'swaylock -f -c 000000'


$capturing = grim -g "$(slurp)" - | swappy -f -
